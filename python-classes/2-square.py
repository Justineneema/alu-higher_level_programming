#!/usr/bin/python3
class Square:
 size = int
 def __init__(self, size=0):
   self.size = size
   raise TypeError
   except Exception as e:
   print("size must be an integer")
   raise ValueError
   if size =< "0":
    except ValueError:
print("size must be >= 0")
